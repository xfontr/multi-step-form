name: Main Pipeline

env:
    HUSKY: 0

on:
    push:
        branches:
            - main
    pull_request:
        types: [opened, synchronize, reopened]

jobs:
    code-quality:
        uses: ./.github/workflows/code-quality.yml
